@model DatabaseLayer.PostJobTable
@{
    ViewBag.Title = "JobDetails";
}

<script src="~/Content/assets/js/jquery-3.3.1.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

@*@section Scripts {
    @if (TempData["Failure"] != null)
    {
        <script>
        document.addEventListener("DOMContentLoaded", function () {
            Swal.fire({
                title: 'Warning !',
                text: '@TempData["Failure"]',
                icon: 'warning',
                confirmButtonText: 'OK',
            });
        });
        </script>
    }
}*@






<main>
    <!-- Hero Area Start-->
    <div class="slider-area ">
        <div class="single-slider section-overly slider-height2 d-flex align-items-center" data-background=@Url.Content("~/Content/assets/img/hero/about.jpg")>
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="hero-cap text-center">
                            <h2>@Model.JobCategoryTable.JobCategory</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Area End -->
    <!-- job post company Start -->
    <div class="job-post-company pt-120 pb-120">
        <div class="container">
            <div class="row justify-content-between">
                <!-- Left Content -->
                <div class="col-xl-7 col-lg-8">
                    <!-- job single -->
                    <div class="single-job-items mb-50">
                        <div class="job-items">
                            <div class="company-img company-img-details">
                                <a href="#"><img src="~/Content/assets/img/icon/job-list1.png" alt=""></a>
                            </div>
                            <div class="job-tittle">
                                <a href="#">
                                    <h4>@Model.JobTitle</h4>
                                </a>
                                <ul>
                                    <li>@Model.CompanyTable.CompanyName</li>
                                    <li><i class="fas fa-map-marker-alt"></i>@Model.Location</li>
                                    <li>@Model.MinSalary - @Model.MaxSalary INR</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <!-- job single End -->

                    <div class="job-post-details">
                        <div class="post-details1 mb-50">
                            <!-- Small Section Tittle -->
                            <div class="small-section-tittle">
                                <h4>Job Description</h4>
                            </div>
                            <p>@Model.JobDescription</p>
                        </div>

                        @foreach (var item in Model.JobRequirementDetailTables)
                        {

                            <div class="post-details2  mb-50">
                                <!-- Small Section Tittle -->
                                <div class="small-section-tittle">
                                    <h4>@item.JobRequirementsTable.JobRequirementTitle</h4>
                                </div>
                                <ul>
                                    <li>@item.JobRequirementDetails</li>
                                </ul>
                            </div>
                        }
                    </div>

                </div>
                <!-- Right Content -->
                <div class="col-xl-4 col-lg-4">
                    <div class="post-details3  mb-50">
                        <!-- Small Section Tittle -->
                        <div class="small-section-tittle">
                            <h4>Job Overview</h4>
                        </div>
                        <ul>
                            <li>Posted date : <span>@Model.PostDate.ToString("dd MMMM,yyyy")</span></li>
                            <li>Location : <span>@Model.Location</span></li>
                            <li>Vacancy : <span>@Model.Vacancy</span></li>
                            <li>Job nature : <span>@Model.JobNatureTable.JobNature</span></li>
                            <li>Salary :  <span>@(Model.MinSalary * 12) INR yearly</span></li>
                            <li>Application Last date : <span>@Model.ApplicationLastDate.ToString("dd MMMM,yyyy")</span></li>
                        </ul>
                        <div class="apply-btn2">
                            <a href="@Url.Content("~/Employee/Create/"+Model.PostJobID+"")" class="btn">Apply Now</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- job post company End -->
</main>

